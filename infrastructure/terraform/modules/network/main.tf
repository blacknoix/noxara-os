# Network module skeleton (VPC / subnets) — Phase 0 placeholder.

variable "name" {
  type    = string
  default = "companyos"
}

output "placeholder" {
  value = "Define VPC resources in a later phase."
}
