# Platform service group (Phase 1+)
#
# Search, files (MinIO), notifications, workflow workers (Temporal), and
# analytics ingestion will live here. Phase 1 deployment may co-locate with
# other groups; network boundaries remain logical even when co-deployed.
#
# Phase 0: placeholder only — OpenSearch/ClickHouse may be in compose but are
# not required for the hello vertical slice.
